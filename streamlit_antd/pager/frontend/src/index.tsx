import React from "react"
import ReactDOM from "react-dom"
import STPager from "./STPager"

ReactDOM.render(
  <React.StrictMode>
    <STPager />
  </React.StrictMode>,
  document.getElementById("root")
)
