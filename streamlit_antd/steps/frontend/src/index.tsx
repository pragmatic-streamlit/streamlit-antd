import React from "react"
import ReactDOM from "react-dom"
import STSteps from "./STSteps"
import 'antd/dist/antd.css'; 

ReactDOM.render(
  <React.StrictMode>
    <STSteps />
  </React.StrictMode>,
  document.getElementById("root")
)
